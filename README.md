Press the "+" button on the website to create a client that connects to matrix.org

# to run the development server:
`trunk serve`

# external build dependencies: 
- `cargo install trunk`
- Emscripten (emcc) for olm-sys used by matrix-sdk